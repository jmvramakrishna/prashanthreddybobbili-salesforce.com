<apex:page controller="Dynamicsoql" >
    <apex:form >
   <apex:pageBlock >
       <apex:pageBlockSection columns="5">
        <apex:outputLabel >accountname</apex:outputLabel>
       <apex:selectList >
           <apex:selectOptions value="{!accname}"/>
           </apex:selectList>
       <apex:selectList >
           <apex:selectOptions value="{!acc}"/>
           </apex:selectList>
       <apex:outputLabel >entervalue</apex:outputLabel>
       <apex:inputtext />
           </apex:pageBlockSection>
       <apex:commandButton value="getdata" action="{!}"/>
      <!---- <apex:pageBlockTable value="{!}" var="a">
           <apex:column value="{!a.}"/>
           <apex:column value="{!a.}"/>
           <apex:column value="{!a.}"/>
       </apex:pageBlockTable>---------->
        </apex:pageBlock>
      </apex:form>
</apex:page>